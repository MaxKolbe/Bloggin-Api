<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" 
  rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" 
  crossorigin="anonymous">
  <title>Blogs</title>
  <style>
    *{
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mb-5">Blog Articles</h1>
    <a href="/blogs/new" class="btn btn-warning">Create Article</a>
  
  <% articles.forEach(article => { %>
    <div class="card mt-2">
      <div class="card-body">
        <h1 class="card-title"> <%= article.title %></h1> 
        <h5 style="margin-top: 17px;"><%= article.author %></h5>
        <div class="card-subtitle mb-2"><%= article.timestamp%></div>
        <div class="card-subtitle mb-2"><%= article.state %></div>
        <div class="card-subtitle mb-2">Read Count: <%= article.read_count %></div>
        <div class="card-subtitle mb-2">Read Time: <%= article.reading_time %>secs</div>
        <div class="card-subtitle mb-2">Tags: <%= article.tags %></div>
        <div class="card-text mb-2">Description: <%= article.description %> </div>
        <div class="card-text mb-2"> <%= article.body %> </div>
        <a href="blogs/<%= article.id%>" class="btn btn-danger">Read More</a>
        <form action="/blogs/<%=article.id%>?_method=DELETE" method="POST" class="d-inline">
          <button type="submit" class="btn btn-success">Delete</button>
          <a href="/blogs/edit/<%=article.id%>" class="btn btn-secondary">Edit</a>
        </form>
      </div>
    </div>
  <% }) %>
  </div>
</body>
</html>